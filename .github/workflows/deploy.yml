name: Deploy to Cloudflare Workers

on:
  push:
    branches:
      - main
    paths:
      - "apps/**"
      - "packages/**"
      - ".github/workflows/deploy.yml"

concurrency:
  group: "publish"
  cancel-in-progress: false

jobs:
  deploy:
    environment: "production"
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
      actions: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Check types
        run: bun run check-types

      - name: Get DB Cert File
        run: curl --create-dirs -o $HOME/.postgresql/root.crt '${{ env.PAGEDECK_DATABASE_CERT_URL }}'
        env:
          PAGEDECK_DATABASE_CERT_URL: ${{ secrets.PAGEDECK_DATABASE_CERT_URL }}

      - name: Database Migration
        run: bun run db:migrate
        env:
          PAGEDECK_DATABASE_URL: ${{ secrets.PAGEDECK_DATABASE_URL }}
          NODE_ENV: production
        working-directory: ./packages/pagedeck/db

      - name: Deploy Application
        run: |
          bun run deploy
        env:
          # Alchemy
          ALCHEMY_PASSWORD: ${{ secrets.ALCHEMY_PASSWORD }}
          ALCHEMY_STATE_STORE: cloudflare
          ALCHEMY_STATE_TOKEN: ${{ secrets.ALCHEMY_STATE_TOKEN }}
          # Cloudflare
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_EMAIL: ${{ secrets.CLOUDFLARE_EMAIL }}
          # PageDeck Server
          PAGEDECK_DATABASE_URL: ${{ secrets.PAGEDECK_DATABASE_URL }}
          PAGEDECK_BETTER_AUTH_SECRET: ${{ secrets.PAGEDECK_BETTER_AUTH_SECRET }}
          PAGEDECK_BETTER_AUTH_URL: ${{ vars.PAGEDECK_BETTER_AUTH_URL }}
          PAGEDECK_CORS_ORIGIN: ${{ vars.PAGEDECK_CORS_ORIGIN }}
          PAGEDECK_POLAR_ACCESS_TOKEN: ${{ secrets.PAGEDECK_POLAR_ACCESS_TOKEN }}
          PAGEDECK_POLAR_SUCCESS_URL: ${{ vars.PAGEDECK_POLAR_SUCCESS_URL }}
          # PageDeck Web
          PAGEDECK_VITE_SERVER_URL: ${{ vars.PAGEDECK_VITE_SERVER_URL }}
          # Common
          NODE_ENV: production
          STAGE: prod
